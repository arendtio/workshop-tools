# workshop-tools

AI Workshop Sandbox workbench (`workshop-sandbox/`) and a Node API (`server/`) for plan validation and OpenAI Realtime client secrets. See [AGENTS.md](AGENTS.md) for run and test commands.
